```typescript PaymentGroupsControllerCancel.ts
import { SDK } from "openapi";
import { PaymentGroupsControllerCancelRequest } from "openapi/dist/models/operations";

(async() => {
  const sdk = new SDK({
    bearer: "",
  });
const id: string = "string";

  const res = await sdk.paymentGroups.paymentGroupsControllerCancel(id);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
