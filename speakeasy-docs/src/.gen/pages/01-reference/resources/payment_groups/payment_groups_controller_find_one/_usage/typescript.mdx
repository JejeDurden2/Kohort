```typescript PaymentGroupsControllerFindOne.ts
import { SDK } from "openapi";
import { PaymentGroupsControllerFindOneRequest } from "openapi/dist/models/operations";

(async() => {
  const sdk = new SDK({
    bearer: "",
  });
const id: string = "string";

  const res = await sdk.paymentGroups.paymentGroupsControllerFindOne(id);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
