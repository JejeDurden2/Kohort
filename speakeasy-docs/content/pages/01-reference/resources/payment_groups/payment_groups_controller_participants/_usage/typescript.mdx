```typescript PaymentGroupsControllerParticipants.ts
import { SDK } from "openapi";
import { PaymentGroupsControllerParticipantsRequest } from "openapi/dist/models/operations";

(async() => {
  const sdk = new SDK({
    bearer: "",
  });
const id: string = "string";

  const res = await sdk.paymentGroups.paymentGroupsControllerParticipants(id);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
