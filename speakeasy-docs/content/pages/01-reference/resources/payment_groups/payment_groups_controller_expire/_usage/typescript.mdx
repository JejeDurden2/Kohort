```typescript PaymentGroupsControllerExpire.ts
import { SDK } from "openapi";
import { PaymentGroupsControllerExpireRequest } from "openapi/dist/models/operations";

(async() => {
  const sdk = new SDK({
    bearer: "",
  });
const id: string = "string";

  const res = await sdk.paymentGroups.paymentGroupsControllerExpire(id);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
