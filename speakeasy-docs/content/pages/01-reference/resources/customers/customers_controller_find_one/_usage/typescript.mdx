```typescript CustomersControllerFindOne.ts
import { SDK } from "openapi";
import { CustomersControllerFindOneRequest } from "openapi/dist/models/operations";

(async() => {
  const sdk = new SDK({
    bearer: "",
  });
const id: string = "string";

  const res = await sdk.customers.customersControllerFindOne(id);

  if (res.statusCode == 200) {
    // handle response
  }
})();
```
